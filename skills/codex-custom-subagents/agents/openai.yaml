interface:
  display_name: "Custom Subagent Delegation"
  short_description: "Delegate Codex tasks to custom model workers"
  default_prompt: "Use $codex-custom-subagents to split bounded work into a task pool and delegate it to the configured custom subagent model."
